# GitHub Pagesで無料公開する手順

このガイドでは、Felice Vita CafeのホームページをGitHub Pagesで無料公開する具体的な手順を説明します。

## 📋 事前準備

- GitHubアカウント（まだお持ちでない場合は作成してください）
- Node.jsがインストールされていること（ビルドに必要）

## 🚀 ステップバイステップ手順

### ステップ1: GitHubアカウントを作成（まだの場合）

1. https://github.com/ にアクセス
2. 「Sign up」をクリック
3. ユーザー名、メールアドレス、パスワードを入力して登録

---

### ステップ2: GitHubでリポジトリを作成

1. GitHubにログイン
2. 右上の「+」アイコンをクリック → 「New repository」を選択
3. リポジトリ名を入力（例: `felice-vita-cafe`）
4. 「Public」を選択（無料でGitHub Pagesを使用するため）
5. 「Add a README file」のチェックは**外す**（既にコードがあるため）
6. 「Create repository」をクリック

**重要**: リポジトリ名をメモしておいてください（例: `felice-vita-cafe`）

---

### ステップ3: プロジェクトをGitに登録してGitHubにプッシュ

ターミナル（コマンドライン）で以下のコマンドを順番に実行してください。

#### 3-1. プロジェクトディレクトリに移動

```bash
cd /Users/horikoshoudai/Downloads/project-4114093
```

#### 3-2. Gitリポジトリを初期化

```bash
git init
```

#### 3-3. .gitignoreファイルを作成（まだない場合）

以下のファイルはGitに含めないようにします：

```bash
cat > .gitignore << 'EOF'
node_modules/
out/
dist/
.DS_Store
*.log
.env
.env.local
EOF
```

#### 3-4. すべてのファイルを追加

```bash
git add .
```

#### 3-5. 初回コミット

```bash
git commit -m "Initial commit: Felice Vita Cafe website"
```

#### 3-6. メインブランチに名前を変更

```bash
git branch -M main
```

#### 3-7. GitHubリポジトリに接続

**重要**: `あなたのユーザー名` と `リポジトリ名` を実際の値に置き換えてください

```bash
git remote add origin https://github.com/あなたのユーザー名/リポジトリ名.git
```

例: `git remote add origin https://github.com/tanaka/felice-vita-cafe.git`

#### 3-8. GitHubにプッシュ

```bash
git push -u origin main
```

**注意**: 初回はGitHubのユーザー名とパスワード（またはPersonal Access Token）の入力を求められます。

---

### ステップ4: GitHub Pagesの設定

1. GitHubのリポジトリページ（例: `https://github.com/あなたのユーザー名/リポジトリ名`）にアクセス
2. 上部のタブから「**Settings**」をクリック
3. 左サイドバーで「**Pages**」をクリック
4. 「Source」セクションで「**GitHub Actions**」を選択
5. 設定は自動的に保存されます

---

### ステップ5: 自動デプロイの確認

1. リポジトリページの上部タブから「**Actions**」をクリック
2. 初回は自動的にデプロイが開始されます
3. 「Deploy to GitHub Pages」というワークフローが表示されます
4. クリックして詳細を確認すると、ビルドの進行状況が見えます
5. すべてのステップが✅（緑色）になれば完了です

**所要時間**: 通常2〜5分程度

---

### ステップ6: サイトにアクセス

デプロイが完了すると、以下のURLでサイトにアクセスできます：

```
https://あなたのユーザー名.github.io/リポジトリ名/
```

例: `https://tanaka.github.io/felice-vita-cafe/`

**重要**: 
- 初回のデプロイ後、URLにアクセスできるまで数分かかる場合があります
- リンクが404エラーになる場合は、10分ほど待ってから再度アクセスしてください

---

## 🔄 コードを更新する場合

コードを更新してサイトを更新するには：

1. ファイルを編集
2. ターミナルで以下を実行：

```bash
cd /Users/horikoshoudai/Downloads/project-4114093

git add .
git commit -m "更新内容の説明"
git push origin main
```

3. GitHub Actionsが自動的にデプロイを開始します
4. Actionsタブでデプロイの進行状況を確認できます

---

## ⚠️ よくある問題と解決方法

### 問題1: `git push` で認証エラーが出る

**解決方法**:
- GitHubでPersonal Access Tokenを作成して使用します
- 手順: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token
- スコープで `repo` にチェックを入れる
- 生成されたトークンをパスワードの代わりに入力

### 問題2: サイトが404エラーになる

**解決方法**:
- GitHub Actionsのデプロイが完了しているか確認（Actionsタブを確認）
- リポジトリ名にタイポがないか確認
- URLは大文字小文字を区別します
- 10〜15分待ってから再度アクセス

### 問題3: 画像が表示されない

**解決方法**:
- 画像のURLが正しいか確認
- 外部URL（readdy.ai）を使用している場合は、そのサービスがアクセス可能か確認

### 問題4: GitHub Actionsのデプロイが失敗する

**解決方法**:
- Actionsタブでエラーメッセージを確認
- `npm ci` や `npm run build` が失敗している場合、ローカルで以下を実行して確認：

```bash
npm install
npm run build
```

---

## 📝 vite.config.tsの確認

GitHub Pagesで公開する場合、リポジトリ名がURLに含まれる場合は `base` を設定する必要があります。

もしサイトのURLが `https://あなたのユーザー名.github.io/` の直下なら、`base: '/'` のままで問題ありません。

もしリポジトリ名が含まれる場合（例: `https://あなたのユーザー名.github.io/リポジトリ名/`）、`vite.config.ts` を以下のように変更してください：

```typescript
base: '/リポジトリ名/',
```

**注**: 現在の設定（`base: '/'`）でリポジトリ名が含まれるURLになる場合でも、GitHub Actionsが自動的にリダイレクトを設定するため、多くの場合は変更不要です。

---

## 🎉 完了！

これで無料でサイトが公開されました！

サイトのURLをブックマークしておくと便利です。

質問や問題があれば、GitHubのIssues機能を使って記録することもできます。
